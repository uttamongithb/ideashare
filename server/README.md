# IdeaShare - Server

Minimal Express + MongoDB backend.

Setup:

1. Copy `.env.example` to `.env` and update `MONGO_URI` and `JWT_SECRET`.
2. Install dependencies:

```
cd server
npm install
```

3. Run dev server:

```
npm run dev
```

API endpoints live under `/api/auth` and `/api/ideas`.
